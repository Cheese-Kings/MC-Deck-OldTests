import type { MultiplePlayersArgument, RecipeType } from "../arguments/index";
import type { Datapack } from "../datapack/index";
export declare class Recipe<P1 extends string, P2 extends string, P3 extends string> {
    private datapack;
    private path;
    recipe: RecipeType<P1, P2, P3>;
    constructor(datapack: Datapack, name: string, recipe: RecipeType<P1, P2, P3>);
    get name(): string;
    /** Give this recipe to the given players. */
    give: (targets: MultiplePlayersArgument) => void;
    /** Take this recipe from the given players. */
    take: (targets: MultiplePlayersArgument) => void;
}
