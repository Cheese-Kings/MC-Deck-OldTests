import type { LiteralUnion } from "../generalTypes";
import type { JsonTextComponent, MultipleEntitiesArgument, OBJECTIVE_CRITERION } from "../arguments/index";
import type { CommandsRoot } from "../commands/index";
import { JsonTextComponentClass } from "./JsonTextComponentClass";
import { PlayerScore } from "./PlayerScore";
export declare class ObjectiveClass {
    private commandsRoot;
    name: string;
    criterion: string;
    display: JsonTextComponentClass | undefined;
    _displayRaw: JsonTextComponent | undefined;
    constructor(commandsRoot: CommandsRoot, name: string, criterion: LiteralUnion<OBJECTIVE_CRITERION>, display?: JsonTextComponent);
    toString(): string;
    ScoreHolder(scoreHolder: MultipleEntitiesArgument): PlayerScore;
}
export declare function Objective(commandsRoot: CommandsRoot, name: string, criterion: string, display?: JsonTextComponent): ObjectiveClass;
