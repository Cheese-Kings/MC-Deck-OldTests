import type { CommandsRoot } from "../commands/index";
import type { ConditionClass } from "../variables/index";
export declare function conditionToString(condition: ConditionType): string;
export declare function getConditionsObjective(commandsRoot: CommandsRoot): import("../variables/index").ObjectiveClass;
export declare class CombinedConditions {
    private values;
    private operator;
    private commandsRoot;
    private static id;
    constructor(commandsRoot: CommandsRoot, values: (ConditionType)[], operator: 'not' | 'and' | 'or');
    removeOr: () => CombinedConditions;
    simplify: () => ConditionType;
    toExecutes: (inverted?: boolean) => {
        requiredExpressions: string[][];
        callableExpression: string[];
    };
    toString(): string;
}
export declare type ConditionType = CombinedConditions | ConditionClass;
