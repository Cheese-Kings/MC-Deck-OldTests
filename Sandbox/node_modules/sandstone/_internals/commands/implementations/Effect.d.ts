import type { LiteralUnion } from "../../generalTypes";
import type { MOB_EFFECTS, MultipleEntitiesArgument } from "../../arguments/index";
import { Command } from "../Command";
/** Add or remove status effects on players and other entities. */
export declare class Effect extends Command {
    /**
     * Gives an effect.
     *
     * @param targets Specifies the target(s).
     *
     * @param effect Specifies the effect to be added.
     *
     * @param seconds
     * Specifies the effect's duration in seconds
     * (or in gameticks for `instant_damage`, `instant_health`, and `saturation`).
     * If not specified, defaults to 30 seconds (or 1 gametick for `instant_damage`, `instant_health`, and `saturation`).
     *
     * It must be between `0` and `1_000_000` (inclusive).
     *
     * @param amplifier
     * Specifies the number of additional levels to add to the effect.
     * If not specified, defaults to `0`.
     *
     * Note that the first tier of a status effect (e.g. Regeneration I) is `0`, so the second tier,
     * for example Regeneration II, would be specified by an amplifier level of `1`.
     *
     * @param hideParticles
     * Specifies whether the particles and the HUD indicator‌ of the status effect should be hidden.
     * If not specified, defaults to `false`.
     */
    give: (targets: MultipleEntitiesArgument, effect: LiteralUnion<MOB_EFFECTS>, seconds?: number | undefined, amplifier?: number | undefined, hideParticles?: boolean | undefined) => void;
    /**
     * Removes an effect.
     *
     * @param targets Specifies the target(s). If unspecified, defaults to `@s`.
     *
     * @param effect Specifies the effect to be removed. If unspecified, clears all effects.
     */
    clear: (targets?: string | import("../../arguments/index")._ShowAlias | import("../../index").SelectorClass<true, true> | import("../../index").SelectorClass<true, false> | import("../../index").SelectorClass<false, false> | import("../../index").SelectorClass<false, true> | undefined, effect?: (Pick<string, never> & {
        trimStart: () => string;
    }) | "minecraft:wither" | "minecraft:hero_of_the_village" | "minecraft:levitation" | "minecraft:speed" | "minecraft:slowness" | "minecraft:haste" | "minecraft:mining_fatigue" | "minecraft:strength" | "minecraft:instant_health" | "minecraft:instant_damage" | "minecraft:jump_boost" | "minecraft:nausea" | "minecraft:regeneration" | "minecraft:resistance" | "minecraft:fire_resistance" | "minecraft:water_breathing" | "minecraft:invisibility" | "minecraft:blindness" | "minecraft:night_vision" | "minecraft:hunger" | "minecraft:weakness" | "minecraft:poison" | "minecraft:health_boost" | "minecraft:absorption" | "minecraft:saturation" | "minecraft:glowing" | "minecraft:luck" | "minecraft:unluck" | "minecraft:slow_falling" | "minecraft:conduit_power" | "minecraft:dolphins_grace" | "minecraft:bad_omen" | undefined) => void;
}
