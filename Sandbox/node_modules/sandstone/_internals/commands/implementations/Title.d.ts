import type { JsonTextComponent, MultiplePlayersArgument } from "../../arguments/index";
import { Command } from "../Command";
export declare class TitleArguments extends Command {
    clear: () => void;
    reset: () => void;
    title: (title: JsonTextComponent) => void;
    subtitle: (subtitle: JsonTextComponent) => void;
    actionbar: (actionbarText: JsonTextComponent) => void;
    times: (fadeIn: number, stay: number, fadeOut: number) => void;
}
export declare class Title extends Command {
    /**
     * Controls text displayed on the screen.
     *
     * @param targets Specifies the player(s) to display a screen title to.
     *
     * @example
     * // Displays a red "Hello World" as title
     * title('@a').title([{text: 'Hello World', color: 'red'}])
     *
     * // Displays a target to kill in the actionbar
     * const target = Selector('@r')
     * title('@a').actionbar(['You target is: ', target])
     */
    title: (targets: MultiplePlayersArgument) => TitleArguments;
}
