#######################
# Debug commands/etc. #
#######################

command /mdDebugName:
    trigger:
        set player's tab name to "Ug Ug the destroyer of worlds"

command /mdDebugVersion:
    trigger:
        broadcast "The verison is being %version%, ok?"

# Reload Mc Deck Skripts
command /mdDebugReload:
    trigger:
        message "no"